import { Theme } from './symbols';

export const defaultTheme: Theme = {
  name: 'default',
  properties: {
    '--backgroundColor': '#fff',
    '--toolbarColor': '#0065A4',
    '--toolbarFont': '#fff',
    '--fontColor': '#444'
  }
};
